---
max_turns: 3
allowed_tools: []
---

Reply with exactly this text and nothing else:

The flight slot stays held until the Human's push lands. The next delivery cannot start until origin has the pushed head, so it waits behind the pending push.
