- Write the two files this block names, the report and the send file, with the editor/write tool, never with `echo`, `printf` or an unquoted heredoc. If your toolset has no editor/write tool, write each with one bash command whose quoted delimiter carries your seat name: `cat > <run-dir>/report-<peer-name>.md <<'END-<peer-name>'`, then the body, then `END-<peer-name>` alone on the last line (the same for `<run-dir>/send-<peer-name>.txt`, and `>>` for the `SEND-FAILED` line below). It expands no `$`, backtick or quote; no body line may equal the delimiter, since lines after an early match run as commands; this form writes only these two files; read each back with `cat` before you send.
- Write the finished report/verdict to `<run-dir>/report-<peer-name>.md`, then print it as the pane's final output.
- At send time, compose a prompt with the verdict/outcome line, a bounded summary (~200 words max), and `full report at <run-dir>/report-<peer-name>.md`. Because that composed text routinely carries backticks, `$`, or quotes, write it to `<run-dir>/send-<peer-name>.txt` and send ONLY `herdr agent prompt <lead-name> "$(cat <run-dir>/send-<peer-name>.txt)"` — the quoting-safe form `herdr-cli.md` mandates for any prompt bearing code, backticks, or `$` — with NO other flags (no `--wait`, no `--no-wait`, nothing). Do not send the report file contents. THIS SEND IS MANDATORY — the report file alone does not notify the Lead.
- If the command exits non-zero: retry it ONCE with exactly the same `"$(cat <run-dir>/send-<peer-name>.txt)"` form (no extra flags); if it still fails, append a line `SEND-FAILED` to the end of the report file and run `herdr notification show "<peer-name>: report send failed" --body "<run-dir>/report-<peer-name>.md" --sound request`, then stop. Do not retry further.
