- Write the finished report/verdict to `<run-dir>/report-<peer-name>.md` with the editor/write tool (never via the shell), then print it as the pane's final output.
- At send time, compose a prompt with the verdict/outcome line, a bounded summary (~200 words max), and `full report at <run-dir>/report-<peer-name>.md`; send ONLY that composed prompt with the EXACT command `herdr agent prompt <lead-name> "<composed prompt>"` and NO other flags (no `--wait`, no `--no-wait`, nothing). Do not send the report file contents. THIS SEND IS MANDATORY — the report file alone does not notify the Lead.
- If the command exits non-zero: retry it ONCE with exactly the same form (no extra flags); if it still fails, append a line `SEND-FAILED` to the end of the report file and run `herdr notification show "<peer-name>: report send failed" --body "<run-dir>/report-<peer-name>.md" --sound request`, then stop. Do not retry further.
