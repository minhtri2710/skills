# Delivery config — <project-slug>

- engineer-kind: <preferred agent kind for Engineer Peers>
- reviewer-kind: <preferred agent kind for the Reviewer Peer>
- engineer-fallback: <pre-chosen fallback Engineer kind for a died or cooled-down Engineer>
- reviewer-fallback: <pre-chosen fallback Reviewer kind>
- engineer-args: <native arguments passed after `--` on `herdr agent start` for the Engineer kind: model selection and the pre-authorization of the read-only and verify commands the charter names, plus `herdr agent prompt <lead-name>`>
- reviewer-args: <the same for the Reviewer kind; read-only inspection and verify commands, plus the report prompt>
- engineer-fallback-args: <native arguments for the fallback Engineer kind: model selection and the same pre-authorizations as engineer-args>
- reviewer-fallback-args: <the same for the fallback Reviewer kind>
# Skill- and extension-isolation flags (`--no-skills`, `--no-extensions` for `pi`) are passed by DOCTRINE at the launch line per `charters.md`, not carried as config `*-args` values.
# A model reasoning-effort flag, where the kind exposes one, rides IN `*-args` after `--` like model selection — claude `--effort low|medium|high|xhigh|max`, agy `--effort low|medium|high`, pi `--thinking off|minimal|low|medium|high|xhigh|max` (or a `--model prov/id:<thinking>` suffix); the writer uses the exact flag its kind exposes. There is no separate effort key: it tunes per-turn reasoning depth only (`project-config.md`, "Precedence and guards").
- checks: <claim-shaped commands, `;`-separated>
- local-ops: <exact named commands the Lead may run on the Human's checkout after a landed merge; ;-separated; dev/disposable data only; never an external write>
- lane-defaults: <path or change class = tiny|normal|high-risk, comma-separated>
- target-line: <default integration product line>
- always-gate: <decisions always routed to the Human, comma-separated>
- worker-cap: <max Engineers in one partition; default 3>
