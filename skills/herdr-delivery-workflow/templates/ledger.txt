LEDGER
Base: <full SHA>
Merge-base: <full SHA>
Partition: <scope name: owned paths, one line per scope; or the solo-Lead scope>
Scope commits: <scope name: full SHA, one line per scope; solo-Lead: the one scope commit>
Acceptance commands: <every command, verbatim>

HEAD <full SHA> | <candidate | repair-<n> | reviewed | integrated>
  Tree: <porcelain and stash list at this head>
  Human gate mailbox: OPEN push/merge/deploy and push-gate/merge-gate/deploy-gate rows require a matching mailbox ATTENTION header when checked with `scripts/gate_row.py --check --ledger <ledger> --mailbox <mailbox>`; without `--mailbox`, `--check` is unchanged.
  Seat-permission constraint: an authorized external write blocked by a classifier/harness is an OPEN `kind=<op>-gate status=open` row with `blocked:seat-permission` in note, `words=none`, and `quote=""`; resolve it with `--resolves=<gate-id>` on the landed `kind=<op>` row, whose note names another executing seat when applicable.
  Dialog attribution: `channel=...:dialog` requires `words=selected` and `status=recorded:<kind>`; only `channel=...:typed` may use `words=human` and `status=resolved:instruction`. `gate_row.py --check` rejects the opposite encoding.
  Kind: <agent kind behind each scope at this head>
  Baseline results: <command: real exit code, one line per command>
  Integrated results: <command: real exit code, one line per command; or n/a>
  Verdict: <PASS | FAIL | BLOCKED> by <reviewer seat, kind> on this exact SHA
  Failures: <causal classification of every failure at this head, or none>
