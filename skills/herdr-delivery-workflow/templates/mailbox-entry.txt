<ISO timestamp>  HEAD <current exact SHA>
ATTENTION <project-slug> <event>: <one line>   (an answer to a Supervisor question carries no ATTENTION prefix)
<the same text the Lead's pane shows — the event fields plus at most a short body; capped at roughly ten lines, with any narrative going to the run's workspace record rather than the mailbox>
