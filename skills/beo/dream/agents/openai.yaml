interface:
  display_name: "Beo Dream"
  short_description: "Maintain and consolidate the shared learning corpus"
  default_prompt: "Use $beo-dream to consolidate accumulated learnings into stable corpus-level guidance, handling deduplication, conflicts, and staleness with explicit evidence and not treating a single feature as corpus work."
