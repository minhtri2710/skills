interface:
  display_name: "Beo Reference"
  short_description: "Shared canonical reference hub for br/bv CLI, status mapping, artifact protocol, pipeline rules, and onboarding-aware workflow references"
  default_prompt: "Look up the exact br or bv CLI syntax, status mapping, artifact protocol, pipeline rule, or onboarding-related workflow reference needed by the calling skill."
