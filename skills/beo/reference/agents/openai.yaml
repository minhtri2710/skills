interface:
  display_name: "Beo Reference"
  short_description: "Shared beo protocol and CLI lookup hub"
  default_prompt: "Use $beo-reference to look up the canonical br/bv CLI syntax, status mapping, artifact protocol, or pipeline rule needed by the calling skill."
