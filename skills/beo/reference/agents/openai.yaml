interface:
  display_name: "Beo Reference"
  short_description: "Return one canonical reference"
  default_prompt: "Use $beo-reference to retrieve one exact shared beo rule, schema, mapping, protocol, template, or command."
