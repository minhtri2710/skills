interface:
  display_name: "Beo Reference"
  short_description: "Shared canonical reference hub for br/bv CLI, status mapping, artifact protocol, and pipeline rules"
  default_prompt: "Look up the exact br or bv CLI syntax, status mapping, artifact protocol, or pipeline rule needed by the calling skill."
