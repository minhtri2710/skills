{
  "$schema": "./registry.schema.json#/definitions/style-policy",
  "description": "Non-authoritative wording policy. Style policy cannot create runtime authority.",
  "forbidden_aliases": {
    "tiny": "invalid density alias",
    "micro": "invalid density alias",
    "small": "invalid density alias",
    "beo_standard": "invalid density alias",
    "PASSEXECUTE": "use PASS_EXECUTE",
    "pass": "invalid readiness/verdict alias",
    "fail": "invalid readiness/verdict alias",
    "ok": "invalid readiness/verdict alias",
    "valid": "use verified only for integrity",
    "clean": "invalid status alias",
    "pending": "invalid as verdict, review status, or debug lifecycle; valid only as item_status",
    "not-started": "use not_started"
  },
  "preferred_terms": {
    "compact": "compact or single-ticket",
    "full": "full",
    "PASS_EXECUTE": "PASS_EXECUTE",
    "Human Gate": "Human Gate"
  }
}
