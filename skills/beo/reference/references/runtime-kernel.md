# Runtime Kernel

Authority: pointer only. The canonical runtime invariants now live in `beo-reference -> references/protocol-core.md`.

Use this file only as a lookup bridge for older managed blocks. New doctrine should cite `protocol-core.md` directly.
