# Loading

Authority: canonical loading and reload rules.

Before owner action, load the active owner `SKILL.md`, `references/skill-contract-common.md`, `FEATURE.json`, and current phase artifacts. Load `registry/pipeline.json` before emitting or checking a `condition_id`.

Reload current artifacts immediately before approval/refusal, product mutation, terminal verdict, owner identity repair, lifecycle closure, Human Gate resolution, or resume after interruption. Templates and chat summaries are advisory and cannot replace current artifacts.
