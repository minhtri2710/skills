interface:
  display_name: "Beo Execute"
  short_description: "Deliver one approved bead"
  default_prompt: "Use $beo-execute to implement and verify exactly one approved ready bead within declared scope."
