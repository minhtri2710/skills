interface:
  display_name: "Beo Review"
  short_description: "Multi-specialist review and quality gate"
  default_prompt: "Use $beo-review to run multi-specialist review on the completed feature, classify findings by severity, then run human UAT against locked decisions."
