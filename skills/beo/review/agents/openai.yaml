interface:
  display_name: "Beo Review"
  short_description: "Issue the post-execution quality verdict"
  default_prompt: "Use $beo-review to assess completed current-phase implementation against the locked contract and verification evidence, then issue exactly one verdict: accept, fix, or reject."
