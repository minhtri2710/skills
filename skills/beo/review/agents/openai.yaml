interface:
  display_name: "Beo Review"
  short_description: "Issue the post-execution verdict"
  default_prompt: "Use $beo-review to verify execution evidence, decision/UAT coverage, approval scope, and required checks, then emit exactly one terminal verdict without repairing code or planning artifacts."
