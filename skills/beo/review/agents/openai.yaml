interface:
  display_name: "Beo Review"
  short_description: "Issue the post-execution verdict"
  default_prompt: "Use $beo-review to assess terminal execution evidence against locked contracts and emit accept, fix, or reject without implementing remediation."
