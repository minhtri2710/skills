interface:
  display_name: "Beo Plan"
  short_description: "Research, plan, and decompose into task beads"
  default_prompt: "Use $beo-plan to research the codebase, write discovery and approach artifacts, then create the current-phase contract, story map, and task beads."
