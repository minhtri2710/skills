interface:
  display_name: "Beo Plan"
  short_description: "Define the current-phase solution and bead set"
  default_prompt: "Use $beo-plan to turn a locked CONTEXT.md into discovery, approach, plan, current-phase contract, story map, and current-phase beads without validating, approving, or implementing."
