interface:
  display_name: "Beo Onboard"
  short_description: "Bootstrap and onboard repositories for the beo workflow"
  default_prompt: "Use $beo-onboard to check onboarding status for this repository, apply onboarding if needed, then hand back to beo-route."
