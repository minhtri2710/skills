interface:
  display_name: "Beo Swarm"
  short_description: "Orchestrate parallel worker agents for execution"
  default_prompt: "Use $beo-swarm to orchestrate parallel worker agents for the approved current phase, coordinating via Agent Mail until all tasks complete."
