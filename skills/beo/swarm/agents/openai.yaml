interface:
  display_name: "Beo Swarm"
  short_description: "Coordinate parallel bead execution"
  default_prompt: "Use $beo-swarm to coordinate approved independent current-phase beads with isolated file scopes without implementing as coordinator."
