interface:
  display_name: "Beo Swarm"
  short_description: "Coordinate parallel workers for approved beads"
  default_prompt: "Use $beo-swarm to coordinate parallel workers for approved current-phase beads with non-overlapping scopes, without doing implementation as the coordinator."
