interface:
  display_name: "Beo Debug"
  short_description: "Find one blocker's root cause and unblock it minimally"
  default_prompt: "Use $beo-debug to isolate one non-obvious blocker, apply the smallest verified unblock when appropriate, and return through the correct pipeline path without absorbing normal delivery work."
