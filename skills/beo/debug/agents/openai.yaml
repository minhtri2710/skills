interface:
  display_name: "Beo Debug"
  short_description: "Systematic debugging for blockers and failures"
  default_prompt: "Use $beo-debug to diagnose the root cause of this failure, check known patterns, reproduce, then apply a verified fix."
