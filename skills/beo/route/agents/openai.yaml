interface:
  display_name: "Beo Route"
  short_description: "Choose the next owner"
  default_prompt: "Use $beo-route to reconstruct beo state and select exactly one next owner without repairing readiness or doing downstream work."
