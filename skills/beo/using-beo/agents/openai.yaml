interface:
  display_name: "Beo Using Beo"
  short_description: "Bootstrap and onboard repositories for the beo workflow"
  default_prompt: "Use $beo-using-beo to check onboarding status for this repository, apply onboarding if needed, then hand back to beo-router."
