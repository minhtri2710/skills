interface:
  display_name: "Beo Validate"
  short_description: "Gate current-phase execution readiness"
  default_prompt: "Use $beo-validate to classify current-phase execution readiness and approval state, then emit the canonical ready or remediation outcome without repairing artifacts."
