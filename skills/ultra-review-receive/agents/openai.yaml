interface:
  display_name: "Ultra Review Receive"
  short_description: "Verify and disposition ultra-review findings"
  default_prompt: "Use $ultra-review-receive to verify docs/ultrareview/<report>.md, disposition every finding as CONFIRMED, DISPROVEN, DUPLICATE, BLOCKED, or DEFERRED, and implement only explicitly authorized owner-clean fixes."
policy:
  allow_implicit_invocation: false
